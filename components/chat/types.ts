// Re-export from the canonical source. Import directly from "lib/types" in new code.
export type {
  Role,
  AttachmentKind,
  ChatAttachment,
  ChatArtifact,
  ChatMessageMetrics,
  ChatMessage,
  ChatThread,
  BackendCapabilities,
  ModelCapabilities,
  BackendOption,
  ModelOption,
  ChatSettings,
  ContentSegment,
} from "../../lib/types";
